/**
 * @param {number} a - first element
 * @param {number} b - second element
 * @returns {number} add - result of adding
 * @description returns summ of two numbers
 */
function add(a,b) {
    return a+b;
}

let c = add( 3, 7)
let d = add("cake","jam")

module.exports = add;